export const selectedAddToListFuntion = (array, newItem) => {
    const updatedArray = array.unshift(newItem)
    return updatedArray
}


export const darboardCounts = (array, newItem) => {
    const updatedArray = array.unshift(newItem)
    return updatedArray
}
